package es.themin.empires.cmds;



//###I need to have a think about the coding of this###
/*public class ConfirmCommand implements CommandExecutor{


	/*public String plprefix = empires.plprefix;
	@Override
	public boolean onCommand(CommandSender sender, Command cmd, String Label,String[] args) {
		if (!(sender instanceof Player)) {
			sender.sendMessage("This command can only be run by a player");
			return false;
		}
		Player player = (Player) sender;
		if (!UtilManager.confirms.containsKey(player)) {
			player.sendMessage(plprefix + ChatColor.RED + "You don't have anything to confirm at the moment");
			return false;
		}
		
		return false;
	}*/
	

//}*/
